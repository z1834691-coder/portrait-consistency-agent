# E02 · Agent 对话 Image 2 prompt

- 用途：视觉方向稿（不作为精确中文排版源文件）
- 用例：`ui-mockup`
- 尺寸：1440×900（Image 2 方向稿；1280×800 由 SVG 源导出）
- 生成日期：2026-09-02
- 模型路径：内置 Image 2
- 参考角色：Kimi/Moonshot 截图用于自然语言对话与极简导航的构图参考；Party Rock 截图只用于紫色、米白、黑色的面积层级参考；两张页面草图用于消息、照片和状态事实边界参考。未复制品牌、Logo、摄影、页面资产或截图文字。

```text
Use case: ui-mockup
Asset type: desktop web UI keyframe, 1440x900 visual direction board
Primary request: a premium Chinese C-end portrait-consistency Agent interaction screen “Agent 对话”. Make it a single focused workspace inspired by the simple Kimi/Moonshot conversation layout: a slim near-black/dark-purple navigation rail, a compact context strip, a central photo alignment workspace, and one persistent right-side conversation panel. The user should feel they upload a photo and speak naturally; no separate report page, no parameter dashboard, no multi-step wizard. Purple must be a prominent visual mass, with a dark plum flowing-ribbon field behind the photo workspace and a lighter purple active rail; warm ivory is the readable conversation and photo surface, co-dominant rather than an overwhelming empty background.
Input images: use the user’s Kimi/Moonshot screenshots as references for clean navigation and language-UI focus, the Party Rock screenshot as reference for purple/ivory/black area hierarchy, and the two wireframe screenshots as content constraints. Do not copy any logo, brand, photo, or exact page asset.
Scene/backdrop: matte desktop product workspace with solid color surfaces and layered opaque/low-opacity purple flowing forms on dark plum; no gradients.
Subject: abstract non-identifiable portrait placeholders labeled current photo and reference, a single alignment line, concise chat messages, one quiet evidence block showing “当前状态 / 照片质量 / 同人关系 / 内容安全”, and one composer.
Style/medium: high-fidelity product UI art direction, PingFang SC-like Chinese typography, restrained editorial grid, playful rounded Party Rock geometry, crisp ink rules, subtle tactile depth.
Composition/framing: wide 16:10 viewport; central photo stage carries the visual focus, right conversation carries the action; only one composer and at most one compact scoped confirmation card.
Lighting/mood: sophisticated, direct, calm, trustworthy, expressive without clutter.
Color palette: purple and ivory co-dominant, purple slightly stronger as dark-flow field and active highlights; black structural; coral sparse.
Materials/textures: matte paper-like solid surfaces, crisp lines, layered purple ribbons, no glass.
Text (verbatim): Keep raster text minimal and approximate; exact Chinese copy will be typeset in editable SVG/HTML.
Constraints: no identifiable people, no real photos, no scores, metrics, fake progress, chain-of-thought, Plan A/B/C, separate pages, English slogans, watermark.
Avoid: blue, yellow, neon green, gradients, cyberpunk glow, futuristic grid, dense cards, generic photo-editor controls, repetitive buttons.
```

人工检查：对话是唯一主控制面，照片工作区只承担证据；状态区仅显示关键事实，没有工具调用、逐步 Trace、分数或伪造进度。紫色暗流面积明显增加，米白只承载消息和状态的可读区域；精确文案以 `index.html` 与 `figma-import/e02-agent.svg` 为准。
