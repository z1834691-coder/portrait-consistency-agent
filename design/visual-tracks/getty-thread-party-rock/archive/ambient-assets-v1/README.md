# Ambient assets v1（已归档）

`orbit-paper`、`folded-window`、`ink-garden` 是上一轮由 Image 2 生成的无人物环境素材。按产品负责人 2026-09-03 的反馈，它们已从活动 E01 `/align`、E02 `/align/:session` HTML/SVG 关键帧中移除，不再加载，也不再作为默认视觉方向。

这些文件保留在归档目录，仅用于回滚、比较和 prompt 追溯；当前两张产品关键帧使用纯 Party Rock 米白背景。新建的 K00 封面使用 `cover/artwork/` 中有来源记录的公共领域历史艺术作品，不使用这里的素材。
